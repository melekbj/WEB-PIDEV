{% extends 'baseAdmin.html.twig' %}

{% block title %}Home{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col">
                <h1 class="text-center">Welcome to the Admin Page</h1>
            </div>
        </div>
        <div class="row">
            <div class="col-md-4">
                <a href="{{ path('adminReclamation') }}" class="btn btn-primary btn-lg btn-block">Consulter réclamations</a>
            </div>
            <div class="col-md-4">
                <a  href="{{ path('addreclamationtype') }}" class="btn btn-primary btn-lg btn-block">Ajouter type réclamation</a>
            </div>
            <div class="col-md-4">
                <a  href="{{ path('reclamationstat') }}" class="btn btn-primary btn-lg btn-block">Statistiques réclamation</a>
            </div>
        </div>
    </div>
{% endblock %}
