{% extends 'baseAdmin.html.twig' %}

{% block title %}CategorieStore{% endblock %}

{% block body %}
    <h1>CategorieStore</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie_store.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ categorie_store.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categorie_store_index') }}">back to list</a>

    <a href="{{ path('app_categorie_store_edit', {'id': categorie_store.id}) }}">edit</a>

    {{ include('admin/categorie_store/_delete_form.html.twig') }}
{% endblock %}
